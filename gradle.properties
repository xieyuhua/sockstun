android.enableJetifier=true
android.useAndroidX=true

/* 显式指定堆大小，免得 Gradle 守护进程在构建中途崩掉（一崩就会在项目根目录丢下
   一个几百 MB 的 java_pidNNN.hprof）。CI / docker 构建时容器也至少要给 4 GB。 */
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
